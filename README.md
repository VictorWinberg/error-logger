# Error Logger

## Setup

```
echo DATABASE_URL=postgres://localhost/errlog > .env
npm install
npm start
```

## Database setup

```
createdb errlog
```

**Then go to url: `/api/sync`**

> Setup is complete
